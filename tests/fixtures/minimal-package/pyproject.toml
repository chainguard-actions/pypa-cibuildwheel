[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "cibw-test-pkg"
version = "0.1.0"
description = "Minimal test package for cibuildwheel action tests"
